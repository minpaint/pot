# Git Workflow для Production сервера pot.by

## 📋 Общая информация

**Репозиторий:** https://github.com/minpaint/pot
**Production сервер:** 192.168.37.10 (`/home/django/webapps/potby`)
**SSH ключ:** `~/.ssh/id_ed25519_potby`
**SSH host:** `github-potby` (настроен в `~/.ssh/config`)

**Основные ветки:**
- `main` - основная ветка для production
- `develop` - ветка для разработки и промежуточных версий

## 🎯 Философия workflow

**ВАЖНО:** Мы работаем **напрямую на production сервере** и пушим изменения в GitHub.

```
Production Server (192.168.37.10)
    ↓ разработка
    ↓ git commit
    ↓ git push
GitHub (minpaint/pot)
    ↓ backup / история / синхронизация
```

## 🔧 Настройки Git (уже применены)

```bash
git config core.autocrlf input          # Нормализация line endings для Linux
git config core.fileMode false          # Игнорировать изменения прав доступа
git config user.name "OT_online Developer"
git config user.email "dev@ot-online.local"
```

## 📝 Стандартный workflow

### 1. Проверка текущего состояния

```bash
# Текущая ветка и статус
git status

# Просмотр изменений
git diff

# Просмотр изменённых файлов
git status --short
```

### 2. Коммит изменений в develop

```bash
# Проверка что мы на develop
git checkout develop

# Добавление всех изменений
git add .

# Или добавление конкретных файлов
git add file1.py file2.py

# Создание коммита с подробным описанием
git commit -m "$(cat <<'EOF'
Краткое описание изменений (до 50 символов)

Подробное описание:
- Что изменено
- Зачем изменено
- Какие файлы затронуты

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
EOF
)"

# Push в GitHub
git push origin develop
```

### 3. Перенос изменений в main (когда готово к production)

```bash
# Переключение на main
git checkout main

# Merge из develop
git merge develop

# Push в GitHub
git push origin main
```

### 4. Просмотр истории

```bash
# Последние коммиты
git log --oneline -10

# Граф веток
git log --oneline --graph --all -10

# Коммиты не запушенные в origin
git log origin/develop..HEAD --oneline
```

## 🚨 Критически важные правила

### ❌ ЧТО НИКОГДА НЕ ДЕЛАТЬ:

1. **НЕ коммитить секреты:**
   - `.env` файлы (уже в `.gitignore`)
   - Пароли базы данных
   - SECRET_KEY
   - API ключи
   - SSH ключи

2. **НЕ делать force push без необходимости:**
   ```bash
   git push -f origin main  # ⚠️ ОПАСНО! Только если уверены!
   ```

3. **НЕ удалять `.gitignore` и `.gitattributes`**

4. **НЕ коммитить:**
   - `media/` - пользовательские файлы
   - `logs/` - логи
   - `*.db`, `*.sqlite3` - базы данных
   - `__pycache__/`, `*.pyc` - Python кеш
   - `.claude/settings.local.json` - локальные настройки

### ✅ ЧТО ДЕЛАТЬ:

1. **Проверять изменения перед коммитом:**
   ```bash
   git status
   git diff
   ```

2. **Писать осмысленные commit messages**

3. **Регулярно пушить изменения** (backup + история)

4. **Проверять что не добавили лишнего:**
   ```bash
   git status --short
   ```

## 🎨 Формат commit messages

### Хороший commit message:

```
Добавлена функция отправки email уведомлений

Изменения:
- Создан модуль directory/utils/email_notifications.py
- Добавлена Celery задача для отправки email
- Настроен SMTP сервер в settings_prod.py
- Добавлены шаблоны email в templates/emails/

Технические детали:
- Используется Django email backend
- Асинхронная отправка через Celery
- Поддержка HTML шаблонов

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

### Плохой commit message:

```
git commit -m "fix"
git commit -m "update"
git commit -m "changes"
```

## 📦 Типичные сценарии

### Сценарий 1: Быстрый фикс (hotfix)

```bash
# На develop
git checkout develop
git add fixed_file.py
git commit -m "Исправлен критический баг в модуле X"
git push origin develop

# Если нужно сразу в main
git checkout main
git merge develop
git push origin main
```

### Сценарий 2: Новая функция (feature)

```bash
# Разработка на develop
git checkout develop

# ... работа над функцией ...

git add .
git commit -m "Добавлена новая функция Y"
git push origin develop

# Тестирование на production

# Когда всё работает - merge в main
git checkout main
git merge develop
git push origin main
```

### Сценарий 3: Откат изменений (revert)

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Откатить последний коммит (создаст новый коммит)
git revert HEAD

# Откатить конкретный коммит
git revert abc1234

# Push отката
git push origin develop
```

### Сценарий 4: Исправление последнего коммита

```bash
# Если коммит ещё НЕ запушен
git add forgotten_file.py
git commit --amend

# ⚠️ Если коммит УЖЕ запушен - НЕ используйте --amend!
# Вместо этого создайте новый коммит
```

### Сценарий 5: Просмотр изменений в файле

```bash
# Изменения в конкретном файле
git diff directory/models/employee.py

# История изменений файла
git log --oneline -- directory/models/employee.py

# Кто и когда менял строки в файле
git blame directory/models/employee.py
```

## 🔍 Проверка перед коммитом

**ВСЕГДА выполняйте перед git add:**

```bash
# 1. Проверить статус
git status

# 2. Проверить изменения
git diff

# 3. Убедиться что нет секретов
grep -r "SECRET_KEY\|PASSWORD\|API_KEY" $(git diff --name-only)

# 4. Проверить что .env не добавлен
git status | grep "\.env$" && echo "⚠️ СТОП! .env файл в коммите!" || echo "✅ OK"
```

## 📊 Полезные команды

```bash
# Показать remote URL
git remote -v

# Показать настройки Git
git config --list

# Показать текущую ветку
git branch --show-current

# Показать все ветки (локальные и remote)
git branch -a

# Показать tracking branches
git branch -vv

# Показать статистику коммита
git show --stat HEAD

# Поиск в истории коммитов
git log --grep="email"

# Показать файлы в коммите
git show --name-only abc1234
```

## 🆘 Что делать если...

### Случайно добавили .env файл

```bash
git reset HEAD .env
git status  # проверить что .env убран
```

### Нужно отменить git add (до коммита)

```bash
git reset HEAD file.py
# или отменить всё
git reset HEAD
```

### Случайно сделали коммит не в ту ветку

```bash
# Запомнить hash коммита
git log --oneline -1

# Отменить коммит (сохранив изменения)
git reset --soft HEAD~1

# Переключиться на правильную ветку
git checkout correct-branch

# Закоммитить снова
git add .
git commit -m "Правильный коммит"
```

### Конфликт при merge

```bash
# Git покажет конфликтующие файлы
git status

# Отредактировать файлы, убрав маркеры конфликтов:
# <<<<<<< HEAD
# =======
# >>>>>>> develop

# После исправления:
git add conflicted_file.py
git commit -m "Разрешён конфликт при merge develop в main"
```

### Нужно откатить файл к версии из последнего коммита

```bash
git checkout -- file.py
```

## 📝 Чеклист для Claude при коммите

Когда пользователь просит "закоммить и запушить", выполнять:

- [ ] 1. Проверить текущую ветку (`git status`)
- [ ] 2. Просмотреть изменения (`git diff --stat`)
- [ ] 3. Убедиться что нет секретов в коммите
- [ ] 4. Добавить файлы (`git add .` или конкретные файлы)
- [ ] 5. Создать коммит с осмысленным сообщением
- [ ] 6. Запушить в origin (`git push origin <branch>`)
- [ ] 7. Подтвердить успешный push
- [ ] 8. Показать ссылку на GitHub

## 🔗 Полезные ссылки

- **GitHub репозиторий:** https://github.com/minpaint/pot
- **Ветка develop:** https://github.com/minpaint/pot/tree/develop
- **Ветка main:** https://github.com/minpaint/pot/tree/main
- **История коммитов:** https://github.com/minpaint/pot/commits/develop

## 📚 Дополнительная документация

См. также:
- `GIT_QUICKSTART.md` - быстрый старт по Git
- `CLAUDE.md` - общие инструкции для Claude
- `README.md` - описание проекта

---

**Последнее обновление:** 29.12.2024
**Автор:** Claude Code (Anthropic)
