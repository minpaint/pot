# Автоматический выбор документов при приёме на работу

## Обзор

Система автоматически определяет, какие документы необходимо сгенерировать для сотрудника при приёме на работу на основе характеристик его должности и типа договора.

Логика реализована в функции `get_auto_selected_document_types()` в файле `directory/views/documents/selection.py`.

## Принцип работы

При открытии страницы приёма на работу (`HiringDetailView`) или генерации документов система автоматически:

1. Анализирует характеристики должности сотрудника
2. Определяет необходимый набор документов
3. **Автоматически устанавливает галочки** на соответствующих типах документов
4. Пользователь может снять/добавить галочки вручную перед генерацией

## Правила автоматического выбора

### 1. Распоряжения (`all_orders`)

**Галочка устанавливается автоматически, если:**

- **Срок стажировки > 0** И сотрудник **НЕ подрядчик** (contract_type ≠ 'contractor')

  ИЛИ

- **Управляет служебным автомобилем** (`drives_company_vehicle = True`)

**Что генерируется:**
- Распоряжение о допуске к стажировке
- Распоряжение о допуске к самостоятельной работе
- (Для водителей: стажировка минимум 5 дней по управлению служебным транспортом)

---

### 2. Протокол проверки знаний (`knowledge_protocol`)

**Галочка устанавливается автоматически, если:**

- **Срок стажировки > 0**

  ИЛИ

- **Ответственный за охрану труда** (`is_responsible_for_safety = True`)

  ИЛИ

- **Управляет служебным автомобилем** (`drives_company_vehicle = True`)

**Что генерируется:**
- Протокол первичной проверки знаний по основной должности
- (Для водителей: дополнительная строка в таблице по управлению служебным автомобилем)

---

### 3. Лист ознакомления с документами (`doc_familiarization`)

**Галочка устанавливается автоматически, если:**

- У должности есть **связанные документы** (`position.documents.exists()`)

**Что генерируется:**
- Лист ознакомления со списком документов, инструкций, положений

---

### 4. Карточка учёта СИЗ (`siz_card`)

**Галочка устанавливается автоматически, если:**

- У должности есть **нормы выдачи СИЗ** (`SIZNorm.objects.filter(position=...)`)

**Что генерируется:**
- Личная карточка учёта выдачи СИЗ (DOCX формат)

---

### 5. Личная карточка по охране труда (`personal_ot_card`)

**Галочка устанавливается автоматически, если:**

- Тип договора = **Подряд** (`contract_type = 'contractor'`)

**Что генерируется:**
- Личная карточка прохождения обучения по охране труда

---

### 6. Образец заполнения журнала (`journal_example`) ⭐

**Галочка устанавливается ВСЕГДА для ВСЕХ сотрудников.**

**Что генерируется:**
- Образец заполнения журнала регистрации инструктажа на рабочем месте

---

## Особенности для водителей служебного автомобиля 🚗

### Критерий определения

Сотрудник считается **водителем служебного автомобиля**, если у его должности установлен флаг:

```python
position.drives_company_vehicle = True
```

### Автоматически выбираемые документы

Для водителей **ВСЕГДА** автоматически добавляются:

1. ✅ **Распоряжения** (`all_orders`)
   - Даже если у основной должности `internship_period_days = 0`
   - Включает стажировку по управлению служебным автомобилем (минимум 5 дней)

2. ✅ **Протокол проверки знаний** (`knowledge_protocol`)
   - Содержит ДВЕ строки в таблице:
     - Строка 1: проверка знаний по основной должности
     - Строка 2: проверка знаний по управлению служебным автомобилем

3. ✅ **Образец заполнения журнала** (`journal_example`)
   - Как для всех сотрудников

### Дополнительная функциональность для водителей

#### В контексте шаблонов доступна переменная `{{ driving_auto }}`

**Значение:**
- Если `drives_company_vehicle = True`: `" (управление служебным автомобилем)"`
- Если `drives_company_vehicle = False`: `""`

**Использование в шаблонах:**
```
Должность: {{ position_nominative }}{{ driving_auto }}
```

**Результат:**
- Для водителя: "Инженер (управление служебным автомобилем)"
- Для не-водителя: "Инженер"

#### Управление инструкциями по охране труда

В шаблонах доступна переменная `{{ instruction_numbers }}`, которая автоматически объединяет:
- Инструкции по основной должности (`position.safety_instructions_numbers`)
- Инструкции по управлению автомобилем (`position.company_vehicle_instructions`)

**Пример:**
```
Инструкции: {{ instruction_numbers }}
```

**Результат:**
```
Инструкции: ИОТ-001, ИОТ-015, ИОТ-А-001
```

---

## Матрица выбора документов

| Тип сотрудника | Распоряжения | Протокол | Ознакомление | СИЗ | Личная карточка | Журнал |
|---------------|-------------|----------|--------------|-----|----------------|--------|
| **Обычный сотрудник** (стажировка > 0) | ✅ | ✅ | ➖ | ➖ | ➖ | ✅ |
| **Обычный сотрудник** (без стажировки) | ➖ | ➖ | ➖ | ➖ | ➖ | ✅ |
| **Ответственный за ОТ** | ➖ | ✅ | ➖ | ➖ | ➖ | ✅ |
| **Водитель служебного авто** 🚗 | ✅ | ✅ | ➖ | ➖ | ➖ | ✅ |
| **Подрядчик** (стажировка > 0) | ➖ | ✅ | ➖ | ➖ | ✅ | ✅ |
| **Подрядчик** (без стажировки) | ➖ | ➖ | ➖ | ➖ | ✅ | ✅ |
| **С документами** | ? | ? | ✅ | ➖ | ? | ✅ |
| **С нормами СИЗ** | ? | ? | ➖ | ✅ | ? | ✅ |

**Обозначения:**
- ✅ - галочка устанавливается автоматически
- ➖ - галочка НЕ устанавливается
- ? - зависит от других условий

---

## Примеры использования

### Пример 1: Инженер без стажировки

**Характеристики должности:**
```python
position_name = "Инженер"
internship_period_days = 0
drives_company_vehicle = False
is_responsible_for_safety = False
```

**Автоматически выбранные документы:**
- ✅ Образец заполнения журнала

---

### Пример 2: Водитель-инженер

**Характеристики должности:**
```python
position_name = "Инженер"
internship_period_days = 3
drives_company_vehicle = True  # ⭐ Управляет автомобилем
is_responsible_for_safety = False
```

**Автоматически выбранные документы:**
- ✅ Распоряжения (стажировка: 3 дня по должности + 5 дней по автомобилю = max(3, 5) = 5 дней)
- ✅ Протокол проверки знаний (2 строки в таблице)
- ✅ Образец заполнения журнала

**В протоколе:**
| № | ФИО | Профессия/должность | Вид проверки | № билета |
|---|-----|---------------------|--------------|----------|
| 1 | Иванов И.И. | Инженер | первичная | 15 |
| 2 | Иванов И.И. | Управление служебным автомобилем | первичная | 15 |

---

### Пример 3: Электрик с нормами СИЗ и документами

**Характеристики должности:**
```python
position_name = "Электрик 4 разряда"
internship_period_days = 5
drives_company_vehicle = False
is_responsible_for_safety = False
documents.count() = 3  # Есть связанные документы
siz_norms.count() = 5  # Есть нормы СИЗ
```

**Автоматически выбранные документы:**
- ✅ Распоряжения
- ✅ Протокол проверки знаний
- ✅ Лист ознакомления с документами
- ✅ Карточка учёта СИЗ
- ✅ Образец заполнения журнала

---

### Пример 4: Подрядчик (договор подряда)

**Характеристики:**
```python
contract_type = "contractor"
position.contract_work_name = "Ремонт кровли"
internship_period_days = 3  # Игнорируется для подрядчика
```

**Автоматически выбранные документы:**
- ✅ Протокол проверки знаний (если стажировка > 0)
- ✅ Личная карточка по ОТ
- ✅ Образец заполнения журнала

**Особенности:**
- Распоряжения НЕ генерируются (подрядчики не проходят стажировку)
- В документах используется `contract_work_name` вместо `position_name`

---

## Код реализации

### Основная функция

Файл: `directory/views/documents/selection.py`

```python
def get_auto_selected_document_types(employee):
    """
    Автоматически определяет типы документов для генерации
    на основе данных сотрудника.
    """
    document_types = []

    # Получаем характеристики
    is_contractor = getattr(employee, 'contract_type', 'standard') == 'contractor'
    internship_period = getattr(employee.position, 'internship_period_days', 0)
    is_responsible_for_safety = getattr(employee.position, 'is_responsible_for_safety', False)
    drives_company_vehicle = getattr(employee.position, 'drives_company_vehicle', False)

    # Распоряжения
    if internship_period > 0 and not is_contractor:
        document_types.append('all_orders')

    # Протокол
    if internship_period > 0 or is_responsible_for_safety:
        document_types.append('knowledge_protocol')

    # ВОДИТЕЛЬ: всегда распоряжения + протокол
    if drives_company_vehicle:
        if 'all_orders' not in document_types:
            document_types.append('all_orders')
        if 'knowledge_protocol' not in document_types:
            document_types.append('knowledge_protocol')

    # Лист ознакомления
    if hasattr(employee.position, 'documents') and employee.position.documents.exists():
        document_types.append('doc_familiarization')

    # СИЗ
    from directory.models.siz import SIZNorm
    if SIZNorm.objects.filter(position=employee.position).exists():
        document_types.append('siz_card')

    # Подрядчик
    if is_contractor:
        document_types.append('personal_ot_card')

    # ВСЕГДА для всех
    if 'journal_example' not in document_types:
        document_types.append('journal_example')

    return list(set(document_types))
```

---

## Использование в представлениях

### В HiringDetailView

Файл: `directory/views/hiring.py`

```python
def get_context_data(self, **kwargs):
    context = super().get_context_data(**kwargs)

    employee = self.object.employee
    auto_selected = get_auto_selected_document_types(employee)

    context['document_selection_form'] = DocumentSelectionForm(
        initial={
            'employee_id': employee.id,
            'document_types': auto_selected  # Автоматический выбор
        }
    )

    return context
```

---

## Переопределение автоматического выбора

Пользователь может **вручную** изменить выбор документов:

1. Снять галочки с автоматически выбранных документов
2. Добавить галочки на дополнительные документы
3. Нажать кнопку "Сгенерировать документы"

Система сгенерирует **только** те документы, галочки на которых установлены в момент нажатия кнопки.

---

## Связанные файлы

### Генераторы документов

- `directory/document_generators/order_generator.py` - Распоряжения
- `directory/document_generators/protocol_generator.py` - Протоколы
- `directory/document_generators/familiarization_generator.py` - Лист ознакомления
- `directory/document_generators/siz_card_docx_generator.py` - Карточка СИЗ
- `directory/document_generators/ot_card_generator.py` - Личная карточка
- `directory/document_generators/journal_example_generator.py` - Образец журнала

### Утилиты для водителей

- `directory/utils/vehicle_utils.py` - Функции для работы с управлением служебным автомобилем:
  - `needs_vehicle_training(employee)` - проверка необходимости обучения
  - `get_vehicle_position_name()` - название виртуальной должности
  - `get_vehicle_internship_days()` - срок стажировки для водителей
  - `get_vehicle_instructions(employee)` - инструкции по автомобилю
  - `combine_instructions(employee)` - объединение инструкций

### Базовый контекст

- `directory/document_generators/base.py` - Функция `prepare_employee_context()`:
  - Добавляет `driving_auto` в контекст
  - Объединяет инструкции через `combine_instructions()`
  - Подготавливает все переменные для шаблонов

---

## Отладка

### Просмотр логов

Функция логирует результаты автоматического выбора:

```python
logger.info(f"Автоматически выбранные типы документов для {employee.full_name_nominative}: {document_types}")
```

**Пример лога:**
```
INFO Автоматически выбранные типы документов для Иванов Иван Иванович:
['all_orders', 'knowledge_protocol', 'journal_example']
```

### Проверка в Django shell

```python
from directory.models import Employee
from directory.views.documents.selection import get_auto_selected_document_types

emp = Employee.objects.get(id=123)
docs = get_auto_selected_document_types(emp)
print(docs)
# ['all_orders', 'knowledge_protocol', 'journal_example']
```

---

## История изменений

### 2025-01-30
- ✅ Добавлена поддержка водителей служебного автомобиля (`drives_company_vehicle`)
- ✅ Образец заполнения журнала теперь выбирается автоматически для ВСЕХ сотрудников
- ✅ Переменная `{{ driving_auto }}` добавлена в базовый контекст
- ✅ Исправлена генерация протокола: вид проверки "первичная" для приёма на работу

### Предыдущие версии
- Базовая реализация автоматического выбора документов
- Поддержка подрядчиков
- Поддержка ответственных за охрану труда

---

## См. также

- [SESSION_VEHICLE_MANAGEMENT.md](SESSION_VEHICLE_MANAGEMENT.md) - Управление служебным автомобилем
- [PERIODIC_PROTOCOL_NOTES.md](PERIODIC_PROTOCOL_NOTES.md) - Периодический протокол проверки знаний
- [PROJECT_DESCRIPTION.md](PROJECT_DESCRIPTION.md) - Общее описание проекта
