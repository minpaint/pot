# Импорт/экспорт e-mail (организация / подразделение / отдел)

Документ описывает, как массово выгружать и загружать почтовые адреса для уведомлений на уровне организации, структурного подразделения и отдела.

## Где кнопки
- Админка → Справочник → Структурные подразделения (`/admin/directory/structuralsubdivision/`).
- В списке доступны кнопки **📤 Экспорт e-mail** и **📥 Импорт e-mail**.

## Формат файла
- Поддерживаются `XLSX` и `XLS`, один лист.
- Столбцы (порядок фиксирован):
  1. `target_level` — `organization` / `subdivision` / `department`
  2. `org_short_name` — краткое название организации (case-insensitive exact match)
  3. `subdivision_name` — требуется для `subdivision` и `department` (ищется по имени в организации)
  4. `department_name` — требуется для `department` (ищется в связке организация + подразделение; если у отдела subdivision пустой, колонку оставляем пустой)
  5. `email` — адрес (lower/trim, можно пусто для очистки списка)
  6. `description` — опционально
  7. `is_active` — опционально, по умолчанию `true` (`1/true/yes/да/on` → True)

## Экспорт
- Выгружает только доступные по правам организации.
- В файл попадают:
  - Организационные адреса из `EmailSettings.recipient_emails`.
  - Email подразделений (`SubdivisionEmail`).
  - Email отделов (`DepartmentEmail`).
- Если у объекта нет адресов, добавляется строка с пустым `email` для удобства заполнения.

## Импорт
- Доступ ограничен организациями, на которые у пользователя есть права.
- Режимы:
  - Без галочки “Сохранить изменения” — dry-run с проверкой и выводом ошибок.
  - С галочкой — применяет изменения.
- Поведение по уровням:
  - `organization`: полностью заменяет `EmailSettings.recipient_emails` на список из файла (уникально, по одной строке).
  - `subdivision`: удаляет существующие `SubdivisionEmail` и создаёт список из файла для указанных подразделений.
  - `department`: удаляет существующие `DepartmentEmail` и создаёт список из файла для указанных отделов.
  - Пустая строка `email` очищает список адресов объекта.
- Ничего не создаёт: если организация/подразделение/отдел не найден, строка попадает в ошибки.
- Валидации: формат email, дубликаты в файле по объекту, доступность организации, обязательные поля по уровню.

## Пример строки
```
target_level,org_short_name,subdivision_name,department_name,email,description,is_active
organization,ООО РиК,,,common@rik.by,,1
subdivision,ООО РиК,Цех №1,,engineer@rik.by,Главный инженер,1
department,ООО РиК,Цех №1,Участок сборки,master@rik.by,Начальник участка,1
```
