# 📧 Настройка email-уведомлений о медосмотрах

## Обзор системы

Система OT_online автоматически отправляет email-уведомления о медицинских осмотрах:
- **Просроченные медосмотры** - требующие немедленного внимания
- **Предстоящие медосмотры** - в течение 30 дней (настраивается)
- **Без даты** - сотрудники, которым нужно внести дату медосмотра

## 🆕 Новый способ настройки (рекомендуется)

**Теперь SMTP можно настроить через админ-панель!**

Перейдите к новой документации: **[EMAIL_SMTP_ADMIN_SETUP.md](EMAIL_SMTP_ADMIN_SETUP.md)**

### Преимущества настройки через админку:
- ✅ Настройка через веб-интерфейс (не нужно редактировать файлы)
- ✅ Отдельные настройки для каждой организации
- ✅ Встроенная кнопка "Тест email" для проверки
- ✅ Управление списком получателей через интерфейс

---

## Старый способ настройки (через .env файл)

> ⚠️ **Этот способ сохранён для обратной совместимости, но рекомендуется использовать новую систему через админ-панель.**

---

## 1️⃣ Настройка SMTP (Email-сервер)

### Способ 1: Через файл `.env` (рекомендуется)

Создайте файл `.env` в корне проекта или добавьте в существующий:

```env
# 📧 Настройки email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com              # SMTP сервер (Gmail, Яндекс, и т.д.)
EMAIL_PORT=587                         # Порт (587 для TLS, 465 для SSL)
EMAIL_USE_TLS=True                     # Использовать TLS (True/False)
EMAIL_USE_SSL=False                    # Использовать SSL (True/False)
EMAIL_HOST_USER=your-email@gmail.com   # Email отправителя
EMAIL_HOST_PASSWORD=your-app-password  # Пароль приложения
DEFAULT_FROM_EMAIL=your-email@gmail.com # Email "От кого"
```

### Способ 2: Прямо в `settings.py`

```python
# settings.py
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-app-password'
DEFAULT_FROM_EMAIL = 'your-email@gmail.com'
```

---

## 2️⃣ Популярные SMTP настройки

### Gmail
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
```
⚠️ **Важно для Gmail:**
1. Включите двухфакторную аутентификацию
2. Создайте "Пароль приложения": https://myaccount.google.com/apppasswords
3. Используйте этот 16-символьный пароль (не пароль от аккаунта!)

### Яндекс
```env
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@yandex.ru
EMAIL_HOST_PASSWORD=your-password
```

### Mail.ru
```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@mail.ru
EMAIL_HOST_PASSWORD=your-password
```

### Корпоративный Exchange/Office365
```env
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@company.com
EMAIL_HOST_PASSWORD=your-password
```

### Локальный Postfix/Sendmail
```env
EMAIL_HOST=localhost
EMAIL_PORT=25
EMAIL_USE_TLS=False
```

---

## 3️⃣ Настройка периодичности уведомлений

Настраивается **для каждой организации отдельно** в админке:

### Админка Django
`http://localhost:8001/admin/deadline_control/medicalsettings/`

### Параметры:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| **Дней до отметки к выдаче** | За сколько дней до истечения срока показывать статус "к выдаче" | 30 дней |
| **Дней до напоминания на email** | За сколько дней до истечения срока отправлять email | 45 дней |

**Пример:**
- Медосмотр истекает 1 декабря
- `days_before_email = 45` → Email отправится 17 октября
- `days_before_issue = 30` → Статус "к выдаче" появится 1 ноября

---

## 4️⃣ Запуск отправки уведомлений

### Ручной запуск (для теста)

```bash
# Отправить всем администраторам
py manage.py send_medical_notifications

# Отправить на конкретные email
py manage.py send_medical_notifications --emails admin@company.com,hr@company.com

# Только для одной организации (ID организации)
py manage.py send_medical_notifications --organization 1
```

### Автоматическая отправка (через cron/scheduler)

#### Windows Task Scheduler
Создайте задачу, выполняющую:
```cmd
cd "G:\Мой диск\OT_online"
c:\venvs\OT_online\Scripts\python.exe manage.py send_medical_notifications
```
Расписание: 2 раза в месяц (1-го и 15-го числа)

#### Linux Cron
```bash
# Редактировать crontab
crontab -e

# Добавить задачу (1-го и 15-го числа в 9:00)
0 9 1,15 * * cd /path/to/OT_online && /path/to/venv/bin/python manage.py send_medical_notifications
```

#### systemd Timer (Linux)
```ini
# /etc/systemd/system/medical-notifications.timer
[Unit]
Description=Medical Notifications Timer

[Timer]
OnCalendar=*-*-1,15 09:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

---

## 5️⃣ Тестирование email

### Тест 1: Отправка тестового письма из Django shell

```python
py manage.py shell

from django.core.mail import send_mail
from django.conf import settings

send_mail(
    subject='Тест настройки email',
    message='Если вы получили это письмо - настройки работают!',
    from_email=settings.DEFAULT_FROM_EMAIL,
    recipient_list=['your-email@example.com'],
    fail_silently=False,
)
```

### Тест 2: Режим консоли (без отправки)

```env
# .env - для разработки/отладки
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
```
Письма будут выводиться в консоль вместо отправки.

### Тест 3: Файловый backend (сохранение в файлы)

```python
# settings.py
EMAIL_BACKEND = 'django.core.mail.backends.filebased.EmailBackend'
EMAIL_FILE_PATH = BASE_DIR / 'tmp_emails'
```
Письма сохраняются в папку `tmp_emails/`.

---

## 6️⃣ Формат письма

Письмо содержит три раздела:

### 📋 Требуется внести дату медосмотра
- ФИО сотрудника
- Должность
- Организация
- Вредные факторы
- Минимальная периодичность

### 🚨 Просроченные медосмотры
- ФИО сотрудника
- Должность
- Организация
- Вредные факторы
- Дата последнего МО
- Сколько дней просрочено

### ⚠️ Предстоящие медосмотры (скоро истекают)
- ФИО сотрудника
- Должность
- Организация
- Вредные факторы
- Дата следующего МО
- Сколько дней осталось

### ИТОГО
Общая статистика по всем категориям.

---

## 7️⃣ Получатели уведомлений

### По умолчанию
Письма отправляются **всем администраторам** (пользователям с `is_staff=True` и заполненным email).

### Указать получателей вручную
```bash
py manage.py send_medical_notifications --emails admin@company.com,boss@company.com
```

### Где указывать email администраторов
1. Админка Django: `http://localhost:8001/admin/auth/user/`
2. Редактировать пользователя
3. Поле "Адрес электронной почты"

---

## 8️⃣ Устранение проблем

### Ошибка: SMTPAuthenticationError
**Причина:** Неверный логин/пароль
**Решение:**
- Проверьте `EMAIL_HOST_USER` и `EMAIL_HOST_PASSWORD`
- Для Gmail используйте "Пароль приложения", не пароль аккаунта
- Убедитесь что нет лишних пробелов

### Ошибка: SMTPConnectError / Connection refused
**Причина:** Неверный хост или порт
**Решение:**
- Проверьте `EMAIL_HOST` и `EMAIL_PORT`
- Проверьте брандмауэр (порт должен быть открыт)
- Проверьте что TLS/SSL настроены правильно

### Письма не приходят (нет ошибок)
**Причина:** Попали в спам
**Решение:**
- Проверьте папку "Спам"
- Добавьте отправителя в белый список
- Настройте SPF/DKIM записи для домена

### Ошибка: [Errno 11001] getaddrinfo failed
**Причина:** Не удается найти SMTP сервер
**Решение:**
- Проверьте интернет-соединение
- Проверьте правильность `EMAIL_HOST`
- Попробуйте ping smtp.gmail.com

---

## 9️⃣ Безопасность

### ⚠️ НЕ ХРАНИТЕ пароли в settings.py!
Используйте `.env` файл и добавьте его в `.gitignore`

```gitignore
.env
*.env
```

### Для production используйте:
- Переменные окружения сервера
- Vault/Secret Manager (для облачных развертываний)
- Encrypted environment variables

---

## 🔟 Рекомендации

1. **Тестируйте** перед настройкой автоматической отправки
2. **Проверяйте** папку спам у получателей
3. **Настраивайте** периодичность под свои нужды (каждая организация отдельно)
4. **Сохраняйте** логи отправки для отладки
5. **Используйте** корпоративный email если возможно

---

## 📚 Дополнительная информация

- Django Email Documentation: https://docs.djangoproject.com/en/5.0/topics/email/
- Gmail App Passwords: https://support.google.com/accounts/answer/185833
- Отчёт о медосмотрах: `/admin/deadline_control/employeemedicalexamination/`
