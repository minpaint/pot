# 📊 Единый импорт/экспорт справочников

## Содержание
1. [Назначение](#назначение)
2. [Доступ к функционалу](#доступ-к-функционалу)
3. [Экспорт данных](#экспорт-данных)
4. [Импорт данных](#импорт-данных)
5. [Структура файла](#структура-файла)
6. [Примеры использования](#примеры-использования)
7. [Обработка ошибок](#обработка-ошибок)
8. [Техническая информация](#техническая-информация)

---

## Назначение

Система единого импорта/экспорта позволяет:
- **Массово экспортировать** три справочника в один Excel-файл с тремя листами
- **Массово импортировать** данные из одного Excel-файла в три справочника
- **Редактировать** данные в Excel и возвращать их обратно в систему

### Поддерживаемые справочники:
1. **Структура** (Position) - организационная структура и должности
2. **Сотрудники** (Employee) - данные сотрудников
3. **Оборудование** (Equipment) - оборудование организации

---

## Доступ к функционалу

### Через админку Django:

1. Войдите в систему под учетной записью администратора
2. На главной странице админки найдите пункт меню **"📊 Единый импорт/экспорт"**
3. Выберите нужное действие:
   - **📥 Импорт** - загрузка данных из Excel
   - **📤 Экспорт** - выгрузка данных в Excel

### Прямые URL:
- Импорт: `/admin/import/`
- Экспорт: `/admin/export/`

---

## Экспорт данных

### Как экспортировать:

1. Перейдите: **Админка → Единый импорт/экспорт → Экспорт**
2. (Опционально) Выберите организацию для фильтрации
3. Нажмите кнопку **"📤 Экспорт"**
4. Файл `export_all.xlsx` будет загружен

### Что экспортируется:

Один Excel-файл с тремя листами:
- **Лист "Структура"** - все должности с полной иерархией (9 столбцов)
- **Лист "Сотрудники"** - все сотрудники (6 столбцов)
- **Лист "Оборудование"** - всё оборудование (7 столбцов)

### Фильтрация по организации:

Если указать параметр `?organization_id=X`, экспортируются только данные выбранной организации.

Пример: `/admin/export/?organization_id=1`

---

## Импорт данных

### Как импортировать:

1. Перейдите: **Админка → Единый импорт/экспорт → Импорт**
2. **(Опционально)** Выберите **организацию** из выпадающего списка
   - Если выбрана организация, поле `org_short_name_ru` в файле можно **оставить пустым**
   - Выбранная организация будет применена ко всем строкам
3. Нажмите **"Выбрать файл"** и укажите Excel-файл (.xlsx или .xls)
4. Нажмите **"Загрузить и просмотреть"**
5. **Предпросмотр:**
   - Проверьте количество записей для импорта
   - Просмотрите ошибки (если есть)
   - Оцените статистику по каждому листу
6. Нажмите **"✅ Подтвердить импорт"**

### Принцип работы:

1. **Порядок обработки:** Структура → Сотрудники → Оборудование
2. **Атомарность:** Если критический лист (Структура) содержит ошибки, весь импорт откатывается
3. **Предпросмотр обязателен:** Данные не сохраняются без подтверждения
4. **Каскадное создание:** Организации, подразделения, отделы создаются автоматически

---

## Структура файла

### Общие требования:
- Формат: `.xlsx` или `.xls`
- Имена листов строго фиксированы: **"Структура"**, **"Сотрудники"**, **"Оборудование"**
- Первая строка каждого листа - заголовки столбцов
- Пустые листы игнорируются

---

### Лист "Структура" (9 столбцов):

| Столбец | Описание | Обязательное | Пример |
|---------|----------|--------------|--------|
| `org_short_name_ru` | Краткое название организации | ⚠️ Условно* | РиК |
| `subdivision_name` | Структурное подразделение | ❌ Нет | Цех №1 |
| `department_name` | Отдел | ❌ Нет | Участок сборки |
| `position_name` | Название должности | ✅ Да | Слесарь |
| `safety_instructions_numbers` | Номера инструкций по ОТ | ❌ Нет | И-001, И-025 |
| `internship_period_days` | Срок стажировки (дни) | ❌ Нет | 5 |
| `is_responsible_for_safety` | Ответственный за ОТ | ❌ Нет | да/нет |
| `can_be_internship_leader` | Может быть руководителем стажировки | ❌ Нет | да/нет |
| `can_sign_orders` | Может подписывать распоряжения | ❌ Нет | да/нет |

**Примечания:**
- **\* `org_short_name_ru`** - можно **не указывать**, если вы выбрали организацию в форме импорта. В этом случае выбранная организация будет применена ко всем строкам.
- Булевы поля поддерживают значения: да/нет, yes/no, 1/0, true/false
- Если `subdivision_name` не указано, должность привязывается только к организации
- Нельзя указать `department_name` без `subdivision_name`

---

### Лист "Сотрудники" (6 столбцов):

| Столбец | Описание | Обязательное | Пример |
|---------|----------|--------------|--------|
| `hire_date` | Дата приема (ДД.ММ.ГГГГ) | ❌ Нет | 15.01.2020 |
| `org_short_name_ru` | Краткое название организации | ⚠️ Условно* | РиК |
| `subdivision_name` | Структурное подразделение | ❌ Нет | Цех №1 |
| `department_name` | Отдел | ❌ Нет | Участок сборки |
| `position_name` | Название должности | ✅ Да | Слесарь |
| `full_name_nominative` | ФИО (именительный падеж) | ✅ Да | Иванов Иван Иванович |

**Автоматическое заполнение:**
- `start_date` = `hire_date` (если дата приема указана)
- `contract_type` = `'standard'`
- `status` = `'active'`
- `full_name_dative` - автогенерация через pymorphy2

**Поля, не входящие в импорт (заполняются позже через интерфейс):**
- `date_of_birth` - дата рождения (необязательное)
- `place_of_residence` - место проживания (необязательное)

**Примечания:**
- **\* `org_short_name_ru`** - можно **не указывать**, если вы выбрали организацию в форме импорта
- Если дата приема не указана, поля `hire_date` и `start_date` останутся пустыми
- Все необязательные поля можно заполнить позже через интерфейс системы

---

### Лист "Оборудование" (7 столбцов):

| Столбец | Описание | Обязательное | Автозаполнение | Пример |
|---------|----------|--------------|----------------|--------|
| `org_short_name_ru` | Краткое название организации | ⚠️ Условно* | - | РиК |
| `subdivision_name` | Структурное подразделение | ❌ Нет | - | Цех №1 |
| `department_name` | Отдел | ❌ Нет | - | Участок сборки |
| `equipment_name` | Наименование оборудования | ✅ Да | - | Токарный станок 16К20 |
| `inventory_number` | Инвентарный номер (8 цифр) | ❌ Нет | автоген. | 00000001 |
| `maintenance_period_months` | Периодичность ТО (месяцев) | ❌ Нет | - | 12 |
| `last_maintenance_date` | Дата последнего ТО (ДД.ММ.ГГГГ) | ❌ Нет | - | 15.01.2025 |

**Автоматическое заполнение:**
- `inventory_number` - генерируется (00000001, 00000002, ...) если не указан
- `next_maintenance_date` - рассчитывается автоматически
- `maintenance_status` = `'operational'`

**Примечание:**
- **\* `org_short_name_ru`** - можно **не указывать**, если вы выбрали организацию в форме импорта

---

## Примеры использования

### Пример 1: Полный цикл экспорт → редактирование → импорт

1. **Экспорт:**
   ```
   /admin/export/
   → Скачали export_all.xlsx
   ```

2. **Редактирование в Excel:**
   - Открыли файл
   - Добавили 5 новых должностей в лист "Структура"
   - Обновили ФИО 3 сотрудников в листе "Сотрудники"
   - Добавили 2 единицы оборудования в лист "Оборудование"

3. **Импорт:**
   ```
   /admin/import/
   → Загрузили отредактированный файл
   → Проверили предпросмотр
   → Подтвердили импорт
   ```

4. **Результат:**
   - Создано 5 должностей
   - Обновлено 3 сотрудника
   - Создано 2 единицы оборудования

---

### Пример 2: Импорт новой организации

Файл `new_org.xlsx`:

**Лист "Структура":**
```
org_short_name_ru | subdivision_name | department_name | position_name | ...
ООО "Новая"       | Офис             |                 | Директор      | ...
ООО "Новая"       | Офис             |                 | Бухгалтер     | ...
ООО "Новая"       | Производство     | Цех №1          | Токарь        | ...
```

**Лист "Сотрудники":**
```
hire_date  | org_short_name_ru | subdivision_name | position_name | full_name_nominative
01.01.2024 | ООО "Новая"       | Офис             | Директор      | Петров Петр Петрович
15.01.2024 | ООО "Новая"       | Офис             | Бухгалтер     | Сидорова Анна Ивановна
```

**Результат импорта:**
- Создана организация "ООО «Новая»"
- Созданы 2 подразделения, 1 отдел
- Созданы 3 должности
- Созданы 2 сотрудника

---

## Обработка ошибок

### Типы ошибок:

#### 1️⃣ Ошибки валидации (до импорта):

**Отсутствие обязательных полей:**
```
Строка 5: Не указано краткое наименование организации (org_short_name_ru)
```

**Логические ошибки:**
```
Строка 18: Нельзя указать отдел без структурного подразделения
```

**Некорректный формат файла:**
```
Поддерживаются только файлы XLSX и XLS
```

**Отсутствие поддерживаемых листов:**
```
Файл не содержит ни одного поддерживаемого листа. Ожидаются листы: Структура, Сотрудники, Оборудование
```

---

#### 2️⃣ Ошибки импорта (во время создания):

**Критические ошибки в листе "Структура":**
```
🚨 Импорт остановлен. Обнаружены ошибки в критическом листе "Структура". Ошибок: 3
```
→ **Все изменения откатываются** (транзакция)

**Ошибки в других листах:**
→ Строки с ошибками **пропускаются**, остальные импортируются

---

### Статусы листов при предпросмотре:

| Статус | Иконка | Описание | Импорт возможен? |
|--------|--------|----------|------------------|
| `ok` | ✅ | Готов к импорту | Да |
| `warn` | ⚠️ | Есть предупреждения | Да (с ограничениями) |
| `error` | ❌ | Ошибка | Нет (если критический лист) |

---

### Рекомендации по исправлению:

1. Скопируйте сообщения об ошибках из предпросмотра
2. Откройте исходный Excel-файл
3. Найдите указанные строки
4. Исправьте ошибки согласно сообщениям
5. Повторите импорт

---

## Техническая информация

### Используемые технологии:
- **django-import-export** - библиотека для импорта/экспорта
- **openpyxl** - работа с XLSX файлами
- **tablib** - обработка табличных данных
- **pymorphy2** - морфологический анализ (склонение ФИО)

### Структура файлов:

```
directory/
├── services/
│   ├── __init__.py
│   └── global_import.py           # Сервисный слой (парсинг, валидация, импорт/экспорт)
├── admin/
│   ├── __init__.py
│   └── global_import_admin.py     # Админ-view для UI
├── resources/
│   ├── organization_structure.py  # Resource для структуры (Position)
│   ├── employee.py                # Resource для сотрудников
│   └── ...
├── templatetags/
│   └── dict_filters.py            # Фильтры для шаблонов
└── management/
    └── commands/
        └── create_global_import_menu.py  # Команда для создания пункта меню

deadline_control/
└── resources/
    └── equipment.py               # Resource для оборудования

templates/
└── admin/
    └── directory/
        └── global_import/
            ├── import.html        # Форма загрузки файла
            └── import_preview.html  # Предпросмотр импорта
```

### Архитектура:

1. **Сервисный слой** (`directory/services/global_import.py`):
   - `parse_workbook()` - парсинг Excel-файла
   - `dry_run_import()` - предпросмотр импорта
   - `commit_import()` - реальный импорт с транзакцией
   - `export_all_to_workbook()` - экспорт всех справочников

2. **Админ-view** (`directory/admin/global_import_admin.py`):
   - `import_view()` - обработка формы импорта
   - `export_view()` - генерация файла экспорта

3. **Переиспользование Resource**:
   - `OrganizationStructureResource` (directory)
   - `EmployeeResource` (directory)
   - `EquipmentResource` (deadline_control)

### Порядок обработки листов:

**Критически важно:**
```python
PROCESSING_ORDER = ['Структура', 'Сотрудники', 'Оборудование']
```

- Структура → создаёт организации, подразделения, отделы, должности
- Сотрудники → привязываются к должностям из структуры
- Оборудование → привязывается к организационной структуре

### Атомарность импорта:

```python
@transaction.atomic
def commit_import(datasets, organization=None):
    # Все три листа импортируются в одной транзакции
    # Если критический лист падает → откат всех изменений
```

---

## FAQ

### Вопрос: Можно ли импортировать только один лист?
**Ответ:** Да, файл может содержать любую комбинацию листов. Если в файле только "Сотрудники" - импортируются только сотрудники.

### Вопрос: Что будет, если импортировать файл дважды?
**Ответ:**
- **Структура:** Существующие должности обновятся
- **Сотрудники:** Существующие сотрудники (по ФИО) обновятся
- **Оборудование:** Существующее оборудование (по inventory_number) обновится

### Вопрос: Можно ли экспортировать данные одной организации?
**Ответ:** Да, используйте параметр `?organization_id=X` в URL экспорта.

### Вопрос: Какой максимальный размер файла?
**Ответ:** Ограничений нет, но рекомендуется не более 5000 строк на лист для оптимальной производительности.

### Вопрос: Поддерживается ли CSV?
**Ответ:** Нет, только XLSX и XLS форматы.

### Вопрос: Можно ли отменить импорт после подтверждения?
**Ответ:** Нет, после подтверждения данные сохраняются. Используйте предпросмотр перед подтверждением.

---

**Последнее обновление:** 2025-01-27
**Версия системы:** 2.1
