# 🏥 Медосмотры в карточке сотрудника

## Описание

Реализована возможность просмотра и управления медосмотрами непосредственно из карточки сотрудника. Теперь медосмотры доступны в трех местах:

1. ✅ **Inline в админке сотрудника** - таблица медосмотров прямо в карточке
2. ✅ **Отдельная страница медосмотров** - детальный просмотр с возможностью редактирования
3. ✅ **Список всех медосмотров** - общий список по всем сотрудникам

---

## 📋 1. Inline в админке сотрудника

### Где находится:
```
Админка → Справочник → Сотрудники → [Выбрать сотрудника]
```

### Что показывает:

Табличное представление медосмотров внизу карточки сотрудника:

| Вредный фактор | Дата прохождения | Следующий медосмотр | Осталось дней | Статус | Скан справки |
|----------------|------------------|---------------------|---------------|--------|--------------|
| 3.4 - Электромагнитные поля | 15.01.2025 | 15.01.2026 | Осталось 57 дней (зеленый) | ✅ Пройден | [Файл] |
| 4.1 - Физические нагрузки | 15.01.2025 | 15.01.2027 | Осталось 787 дней (зеленый) | ✅ Пройден | - |

### Функции:
- ✅ Просмотр всех медосмотров сотрудника
- ✅ Редактирование даты прохождения
- ✅ Загрузка скана справки
- ✅ Цветовая индикация статуса:
  - 🟢 Зеленый - больше 30 дней
  - 🟠 Оранжевый - меньше 30 дней
  - 🔴 Красный - просрочено
- ❌ Добавление медосмотров вручную ЗАПРЕЩЕНО (создаются автоматически)

### Код:
```python
# directory/admin/employee.py

class EmployeeMedicalExaminationInline(admin.TabularInline):
    model = EmployeeMedicalExamination
    extra = 0
    can_delete = False

    fields = [
        'harmful_factor',
        'date_completed',
        'next_date',
        'days_remaining_display',
        'status_display',
        'medical_certificate',
    ]

    readonly_fields = [
        'next_date',
        'days_remaining_display',
        'status_display',
    ]
```

---

## 📄 2. Отдельная страница медосмотров сотрудника

### URL:
```
http://localhost:8001/deadline-control/medical/employee/{employee_id}/
```

### Где ссылка:

**В админке сотрудника:**
```
[Поле "🏥 Статус медосмотров"]
↓
[Кнопка "📋 Медосмотры сотрудника"]
```

**На странице списка медосмотров:**
```
Нажать на ФИО сотрудника
```

### Что показывает:

**1. Общий статус:**
- Статус медосмотров (с цветовой индикацией)
- Дата прохождения
- Следующий медосмотр
- Минимальная периодичность
- Дней до медосмотра
- Кнопка "📅 Изменить дату медосмотра"

**2. Список вредных факторов:**
Карточки для каждого вредного фактора с:
- Название фактора
- Периодичность
- Дата прохождения
- Следующий осмотр
- Статус (badge)
- Скан справки (если есть)

**3. Модальное окно изменения даты:**
- Поле ввода даты
- Автоматическое применение ко ВСЕМ медосмотрам
- Автоматический расчет следующих дат

### Функции:
- ✅ Просмотр детальной информации
- ✅ Изменение даты медосмотра
- ✅ Цветовая индикация по факторам
- ✅ Ссылки на список медосмотров и выдачу направления
- ✅ Адаптивный дизайн

### Код:
```python
# deadline_control/views/medical.py

class EmployeeMedicalDetailView(LoginRequiredMixin, DetailView):
    model = Employee
    template_name = 'deadline_control/employee_medical_detail.html'

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['medical_status'] = self.object.get_medical_status()
        context['examinations'] = self.object.medical_examinations.all()
        return context
```

---

## 🔗 3. Поле "Статус медосмотров" в админке

### Где находится:
```
Админка → Справочник → Сотрудники → [Выбрать сотрудника]
→ Поле "🏥 Статус медосмотров"
```

### Что показывает:

**Если медосмотры не требуются:**
```
Медосмотры не требуются для данной должности
```

**Если требуется внести дату:**
```
📋 Требуется внести дату
Вредных факторов: 2

[Кнопка: 📋 Медосмотры сотрудника]
[Кнопка: 🏥 Выдать направление]
```

**Если медосмотр просрочен:**
```
🚨 Просрочено 15 дней
Следующий медосмотр: 01.11.2025

[Кнопка: 📋 Медосмотры сотрудника]
[Кнопка: 🏥 Выдать направление]
```

**Если скоро истекает:**
```
⚠️ Скоро истекает (25 дней)
Следующий медосмотр: 15.12.2025

[Кнопка: 📋 Медосмотры сотрудника]
[Кнопка: 🏥 Выдать направление]
```

**Если медосмотр действителен:**
```
✅ Действителен (120 дней)
Следующий медосмотр: 15.03.2026

[Кнопка: 📋 Медосмотры сотрудника]
[Кнопка: 🏥 Выдать направление]
```

### Код:
```python
# directory/admin/employee.py

class EmployeeAdmin(TreeViewMixin, admin.ModelAdmin):
    fields = [
        # ... другие поля
        'medical_status_link',
    ]

    readonly_fields = ['medical_status_link']

    def medical_status_link(self, obj):
        medical_status = obj.get_medical_status()

        if not medical_status:
            return "Медосмотры не требуются"

        # Формирование статуса с цветом
        status = medical_status['status']

        # Ссылки
        employee_medical_url = reverse('deadline_control:medical:employee_detail', args=[obj.id])
        referral_url = reverse('deadline_control:medical:referral_existing_employee', args=[obj.id])

        return format_html(
            '{}<br><br>'
            '<a href="{}" class="button" target="_blank">📋 Медосмотры сотрудника</a> '
            '<a href="{}" class="button" target="_blank">🏥 Выдать направление</a>',
            status_html,
            employee_medical_url,
            referral_url
        )
```

---

## 🎯 Преимущества реализации

### 1. **Удобство для HR/ОТ специалиста:**
- Все данные по медосмотрам в одном месте
- Не нужно переходить на отдельную страницу
- Быстрый доступ к выдаче направления

### 2. **Визуальная индикация:**
- Цветовые коды (красный/оранжевый/зеленый)
- Эмодзи для быстрого распознавания статуса
- Четкое указание количества дней

### 3. **Контроль доступа:**
- Фильтрация по организациям
- Только своих сотрудников
- Superuser видит всех

### 4. **Автоматизация:**
- Медосмотры создаются автоматически при приеме сотрудника
- Следующая дата рассчитывается автоматически
- Статус обновляется автоматически

---

## 📚 Связанные файлы

### Backend:
- `directory/admin/employee.py` - Inline и поле статуса
- `deadline_control/views/medical.py` - DetailView для страницы медосмотров
- `deadline_control/urls.py` - URL для детальной страницы
- `directory/models/employee.py` - Метод `get_medical_status()`
- `deadline_control/models/medical_norm.py` - Модель `EmployeeMedicalExamination`

### Frontend:
- `deadline_control/templates/deadline_control/employee_medical_detail.html` - Страница медосмотров
- `deadline_control/templatetags/medical_filters.py` - Фильтр `abs_value`

---

## 🚀 Использование

### Вариант 1: Через админку (быстрый просмотр)
```
1. Открыть админку → Справочник → Сотрудники
2. Выбрать сотрудника
3. Прокрутить вниз → Секция "🏥 Медицинские осмотры"
4. Редактировать даты прямо в таблице
```

### Вариант 2: Через поле статуса (детальный просмотр)
```
1. Открыть админку → Справочник → Сотрудники
2. Выбрать сотрудника
3. Найти поле "🏥 Статус медосмотров"
4. Нажать "📋 Медосмотры сотрудника"
5. На отдельной странице просмотреть детали и изменить дату
```

### Вариант 3: Через список медосмотров
```
1. Открыть http://localhost:8001/deadline-control/medical/
2. Найти сотрудника в списке
3. Нажать на ФИО → переход на детальную страницу
```

---

## ⚙️ Настройка

### Отключить inline (если не нужен):
```python
# directory/admin/employee.py

class EmployeeAdmin(TreeViewMixin, admin.ModelAdmin):
    # inlines = [EmployeeMedicalExaminationInline]  # Закомментировать
```

### Изменить порядок полей inline:
```python
class EmployeeMedicalExaminationInline(admin.TabularInline):
    fields = [
        'harmful_factor',
        'date_completed',
        'next_date',
        # Добавить/убрать поля
    ]
```

### Скрыть поле статуса:
```python
class EmployeeAdmin(TreeViewMixin, admin.ModelAdmin):
    fields = [
        # Убрать 'medical_status_link' из списка
    ]
```

---

## 🔍 Примеры использования

### Пример 1: Проверить медосмотры конкретного сотрудника
```
Админка → Сотрудники → Иванов И.И.
→ Поле "🏥 Статус медосмотров"
→ Видим: "🚨 Просрочено 15 дней"
→ Нажимаем "📋 Медосмотры сотрудника"
→ Видим детали по каждому фактору
→ Нажимаем "📅 Изменить дату медосмотра"
→ Вводим новую дату → Сохранить
→ Статус обновился на "✅ Действителен"
```

### Пример 2: Выдать направление
```
Админка → Сотрудники → Петров П.П.
→ Поле "🏥 Статус медосмотров"
→ Нажимаем "🏥 Выдать направление"
→ Открывается форма направления
→ Заполняем данные → Генерировать
→ Скачивается DOCX документ
```

### Пример 3: Прикрепить скан справки
```
Админка → Сотрудники → Сидоров С.С.
→ Секция "🏥 Медицинские осмотры" (внизу)
→ В строке нужного фактора:
  - Столбец "Скан справки"
  - Выбрать файл → Загрузить
  - Сохранить сотрудника
```

---

## ✅ Итого

Медосмотры теперь полностью интегрированы в карточку сотрудника с тремя уровнями доступа:

1. **Inline** - быстрый просмотр и редактирование
2. **Поле статуса** - общий обзор с кнопками действий
3. **Детальная страница** - полная информация с удобным интерфейсом

Все три способа работают согласованно и дают полный контроль над медосмотрами сотрудников.
