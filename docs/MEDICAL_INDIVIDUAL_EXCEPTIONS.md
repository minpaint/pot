# 🎯 Индивидуальные исключения для медосмотров

## Проблема

Ситуация: в одном структурном подразделении есть несколько сотрудников с одинаковой должностью, но **разными условиями труда**.

### Пример:

```
Должность: "Грузчик"
Организация: ОАО "Белавто"
Подразделение: Склад

Сотрудники:
├─ Иванов И.И. → работает со штабелером → НУЖЕН медосмотр
└─ Петров П.П. → НЕ работает со штабелером → НЕ НУЖЕН медосмотр

Проблема: у них ОДНА И ТА ЖЕ должность!
```

---

## ✅ Решение: Поле `is_disabled` на уровне медосмотра

Добавлено поле **"Не требуется"** (`is_disabled`) в модель `EmployeeMedicalExamination`.

### Как это работает:

```
1. Для должности "Грузчик" указываем вредный фактор "5.2 - Штабелер"

2. Медосмотры создаются АВТОМАТИЧЕСКИ для ВСЕХ грузчиков:
   ├─ Иванов → медосмотр по фактору 5.2 (is_disabled=False)
   └─ Петров → медосмотр по фактору 5.2 (is_disabled=False)

3. Для Петрова вручную отмечаем "Не требуется":
   └─ Петров → медосмотр по фактору 5.2 (is_disabled=True) ✅

4. Результат:
   ├─ Иванов показывается на странице медосмотров
   └─ Петров НЕ показывается (медосмотр отключен)
```

---

## 📝 Пошаговая инструкция

### Шаг 1: Настройте должность

```
Админка → Справочник → Должности → "Грузчик"
→ Секция "🏥 Вредные факторы должности"
→ Добавить фактор:
   ├─ Вредный фактор: 5.2 - Работа со штабелером
   ├─ Периодичность: 12 мес
   └─ Отключено: ☐ (НЕ отмечено)
→ Сохранить
```

**Результат:** Для ВСЕХ сотрудников с должностью "Грузчик" создадутся медосмотры по фактору 5.2

---

### Шаг 2: Отключите медосмотр для Петрова

#### Способ 1: Через inline в карточке сотрудника

```
Админка → Справочник → Сотрудники → Петров П.П.
→ Прокрутить вниз → Секция "🏥 Медицинские осмотры"
→ Найти строку "5.2 - Работа со штабелером"
→ Поставить галочку "Не требуется" ✅
→ Сохранить сотрудника
```

#### Способ 2: Через админку медосмотров

```
Админка → Контроль сроков → Медосмотры сотрудников
→ Найти запись "Петров | 5.2 - Штабелер"
→ Открыть запись
→ Поставить галочку "Не требуется" ✅
→ Сохранить
```

---

### Шаг 3: Проверка

```
Откройте: http://localhost:8001/deadline-control/medical/

Результат:
✅ Иванов - отображается в списке (медосмотр требуется)
❌ Петров - НЕ отображается (медосмотр отключен)
```

---

## 🔍 Как система фильтрует отключенные медосмотры

### В `Employee.get_medical_status()`:

```python
examinations = self.medical_examinations.filter(
    harmful_factor_id__in=harmful_factor_ids,
    is_disabled=False  # ← Игнорируем отключенные медосмотры
)

if not examinations.exists():
    return None  # Медосмотры не требуются
```

### В админке inline:

Отключенные медосмотры **показываются**, но:
- Выделены серым цветом
- Отмечены badge "Не требуется"
- Не учитываются в расчете статуса

---

## 🎨 Визуальная схема

```
┌──────────────────────────────────┐
│ Должность "Грузчик"              │
│ ├─ Организация: ОАО Белавто      │
│ ├─ Подразделение: Склад          │
│ └─ Вредные факторы:              │
│    └─ 5.2 - Штабелер ✅          │
└──────────┬───────────────────────┘
           │
           ├─────────────────────────────────┬─────────────────────────────────┐
           │                                 │                                 │
           ↓                                 ↓                                 ↓
┌────────────────────────┐     ┌────────────────────────┐     ┌────────────────────────┐
│ Иванов И.И.            │     │ Петров П.П.            │     │ Сидоров С.С.           │
│ Должность: Грузчик     │     │ Должность: Грузчик     │     │ Должность: Грузчик     │
└───────┬────────────────┘     └───────┬────────────────┘     └───────┬────────────────┘
        │                              │                              │
        ↓ автоматически               ↓ автоматически               ↓ автоматически
        │                              │                              │
┌───────────────────────────────────────────────────────────────────────────────────────┐
│ EmployeeMedicalExamination                                                            │
├───────────────────────────────────────────────────────────────────────────────────────┤
│ [1] Иванов → 5.2 - Штабелер → is_disabled=False ✅ → Показывается в списке           │
│ [2] Петров → 5.2 - Штабелер → is_disabled=True  ❌ → НЕ показывается в списке        │
│ [3] Сидоров → 5.2 - Штабелер → is_disabled=False ✅ → Показывается в списке          │
└───────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Где видно отключенные медосмотры

### 1. **Inline в карточке сотрудника** ✅

```
Админка → Сотрудники → Петров
→ Секция "🏥 Медицинские осмотры"

Таблица:
┌──────────────────┬──────────────┬─────────┐
│ Вредный фактор   │ Не требуется │ Статус  │
├──────────────────┼──────────────┼─────────┤
│ 5.2 - Штабелер   │ ✅           │ (серый) │
└──────────────────┴──────────────┴─────────┘
```

### 2. **Детальная страница медосмотров** ✅

```
URL: /deadline-control/medical/employee/{id}/

Карточка:
┌─────────────────────────────────────┐
│ 5.2 - Штабелер [Не требуется]      │
│ (серый фон, полупрозрачный)         │
└─────────────────────────────────────┘
```

### 3. **Админка "Медосмотры сотрудников"** ✅

```
Админка → Контроль сроков → Медосмотры сотрудников
→ Фильтр: можно искать и редактировать
```

### 4. **Список медосмотров** ❌

```
URL: /deadline-control/medical/

Отключенные медосмотры НЕ показываются
(сотрудники с отключенными медосмотрами исключены из списка)
```

---

## 💡 Практические примеры

### Пример 1: Временное исключение

**Ситуация:** Петров временно не работает со штабелером (другая задача)

**Действия:**
1. Открыть карточку Петрова
2. Отметить медосмотр "5.2 - Штабелер" как "Не требуется"
3. Петров исчезнет из списка медосмотров

**Через месяц:** Снять галочку "Не требуется" → Петров снова появится в списке

---

### Пример 2: Постоянное исключение

**Ситуация:** Петров НИКОГДА не работает со штабелером (специфика участка)

**Действия:**
1. Открыть карточку Петрова
2. Отметить медосмотр как "Не требуется"
3. Добавить в "Примечания": "Не работает со штабелером, участок Б"

**Результат:** Медосмотр остается в БД (для истории), но не требуется

---

### Пример 3: Массовое исключение

**Ситуация:** Из 10 грузчиков только 3 работают со штабелером

**Действия:**
1. Настроить должность "Грузчик" с фактором "5.2 - Штабелер"
2. Медосмотры создадутся для всех 10 сотрудников
3. Для 7 сотрудников отметить "Не требуется" (через админку или inline)

**Результат:** На странице медосмотров будут только 3 сотрудника

---

## ⚙️ Технические детали

### Модель:

```python
class EmployeeMedicalExamination(models.Model):
    employee = models.ForeignKey(Employee, ...)
    harmful_factor = models.ForeignKey(HarmfulFactor, ...)
    date_completed = models.DateField(...)
    next_date = models.DateField(...)

    # ← НОВОЕ ПОЛЕ
    is_disabled = models.BooleanField(
        default=False,
        verbose_name="Не требуется",
        help_text="Если отмечено, медосмотр не требуется для данного сотрудника"
    )
```

### Фильтрация:

```python
# В Employee.get_medical_status()
examinations = self.medical_examinations.filter(
    harmful_factor_id__in=harmful_factor_ids,
    is_disabled=False  # ← Игнорируем отключенные
)

# В MedicalExaminationListView
# Сотрудники без активных медосмотров не показываются
```

---

## 🆚 Сравнение с другими подходами

### Подход 1: Разные должности (НЕ подходит)

```
❌ Создать должности:
   ├─ "Грузчик (со штабелером)"
   └─ "Грузчик (без штабелера)"

Проблема: Слишком много должностей, сложно управлять
```

### Подход 2: Отключение на уровне должности (НЕ подходит)

```
❌ PositionMedicalFactor.is_disabled = True

Проблема: Отключается для ВСЕХ сотрудников должности
```

### Подход 3: Отключение на уровне медосмотра (✅ ТЕКУЩИЙ)

```
✅ EmployeeMedicalExamination.is_disabled = True

Преимущества:
- Гибкость на уровне конкретного сотрудника
- Не создаются лишние должности
- Медосмотр остается в БД (для истории)
- Легко включить/выключить
```

---

## ✅ Итого

**Решение:** Добавлено поле `is_disabled` в `EmployeeMedicalExamination`

**Как использовать:**
1. Настроить вредные факторы для должности (создаются для всех)
2. Вручную отметить "Не требуется" для конкретных сотрудников
3. Отключенные медосмотры не учитываются в списках и расчетах

**Преимущества:**
- ✅ Максимальная гибкость (на уровне сотрудника)
- ✅ Не нужно создавать лишние должности
- ✅ Легко включать/выключать
- ✅ История сохраняется
- ✅ Работает для любых случаев

**Недостатки:**
- ⚠️ Требует ручной настройки для каждого сотрудника
- ⚠️ Нужно помнить кому отключать

**Когда использовать:**
- Сотрудники с одинаковой должностью, но разными условиями труда
- Временные исключения (болезнь, другая задача)
- Особые случаи (инвалидность, противопоказания)
