# 🍔 Руководство по управлению доступом к меню

## Обзор системы

Система управления доступом к меню позволяет гибко настраивать видимость пунктов меню для разных пользователей через Django Admin.

## Компоненты системы

### 1. Модель MenuItem (`directory.models.MenuItem`)

Модель для хранения пунктов меню с поддержкой:
- **Иерархии** (parent/children)
- **Расположения** (sidebar, top, both)
- **Иконок** (emoji или CSS классы)
- **URL** (Django URL name или прямой URL)
- **Разделителей** (визуальные секции меню)

### 2. Профиль пользователя (`directory.models.Profile`)

Поле `visible_menu_items` позволяет ограничить доступные пункты меню для пользователя:
- **Пусто** — пользователь видит все активные пункты
- **Выбраны пункты** — пользователь видит только указанные пункты

### 3. Admin интерфейс

**Управление пунктами меню:**
- Раздел: Администрирование → Menu items
- Иерархическое отображение с визуальными индикаторами
- Фильтры по расположению, статусу, родителю

**Настройка прав пользователя:**
- Раздел: Администрирование → Пользователи → [Выбрать пользователя]
- Раздел "🍔 Управление меню" (свернут по умолчанию)
- FilteredSelectMultiple виджет для выбора доступных пунктов

## Быстрый старт

### Шаг 1: Заполнить базу начальными пунктами меню

```bash
py manage.py populate_menu_items
```

Эта команда создаст 15 пунктов меню на основе текущей структуры `base.html`.

**Опции:**
- `--clear` — Удалить все существующие пункты перед заполнением

```bash
py manage.py populate_menu_items --clear
```

### Шаг 2: Настроить видимость для пользователя

1. Перейти в **Admin → Пользователи**
2. Выбрать пользователя
3. Развернуть раздел **"🍔 Управление меню"**
4. Выбрать доступные пункты меню
5. Сохранить

**Важно:** Если не выбрано ни одного пункта, пользователь видит все активные пункты.

### Шаг 3: Интеграция в шаблоны

Использование шаблонных тегов для проверки видимости:

```django
{% load menu_tags %}

{# Получить видимые пункты меню #}
{% get_visible_menu_items 'sidebar' as sidebar_items %}
{% for item in sidebar_items %}
    <li>
        <a href="{{ item.get_url }}">
            {{ item.icon }} {{ item.name }}
        </a>
    </li>
{% endfor %}

{# Проверить видимость конкретного пункта #}
{% if item|is_menu_visible:user %}
    <a href="...">...</a>
{% endif %}

{# Проверить видимость по URL name #}
{% check_url_visibility user 'directory:quiz:quiz_list' as is_visible %}
{% if is_visible %}
    <a href="{% url 'directory:quiz:quiz_list' %}">Экзамены</a>
{% endif %}
```

## Примеры использования

### Пример 1: Ограничить доступ к экзаменам

1. Создать нового пользователя (например, `workshop_user`)
2. В разделе "🍔 Управление меню" выбрать только:
   - Сотрудники
   - Медосмотры
   - Dashboard
3. Сохранить

**Результат:** Пользователь не увидит пункты "Экзамены" и "Комиссии" в боковом меню.

### Пример 2: Скрыть административные функции

1. Для обычного пользователя в верхнем меню оставить только:
   - Принять на работу
   - Выдать направление кандидату
2. Убрать "Администрирование"

**Результат:** Пользователь не увидит кнопку "⚙️ Администрирование" в верхнем меню.

### Пример 3: Создать "упрощенный" интерфейс

Для HR-менеджера, который работает только с наймом:
1. Выбрать только:
   - Сотрудники
   - Приемы на работу
   - Принять на работу (верхнее меню)

**Результат:** Минималистичный интерфейс только для работы с персоналом.

## API модели MenuItem

### Методы

```python
# Получить URL пункта меню
menu_item.get_url()

# Проверить видимость для пользователя
menu_item.is_visible_for_user(user)

# Получить полный путь в иерархии
menu_item.get_full_path()  # "Основное > Сотрудники"

# Получить дочерние элементы
menu_item.get_children()

# Проверить наличие дочерних элементов
menu_item.has_children()
```

### Логика видимости

```python
def is_visible_for_user(self, user):
    # Неактивные пункты не видны никому
    if not self.is_active:
        return False

    # Проверка авторизации
    if self.requires_auth and not user.is_authenticated:
        return False

    # Суперпользователи видят все
    if user.is_superuser:
        return True

    # Проверяем права через профиль
    if hasattr(user, 'profile'):
        profile = user.profile
        # Если нет явных ограничений - показываем все
        if not profile.visible_menu_items.exists():
            return True
        # Если есть ограничения - проверяем доступ
        return self in profile.visible_menu_items.all()

    # По умолчанию показываем
    return True
```

## Структура меню в базе данных

После выполнения `populate_menu_items` создаются:

**Боковое меню (sidebar):**
- Основное (раздел)
  - 👥 Сотрудники
  - ➕ Приемы на работу
- ⏰ Контроль сроков (раздел)
  - 📊 Dashboard
  - ⚙️ ТО оборудования
  - 📅 Ключевые сроки
  - 🏥 Медосмотры
- Дополнительно (раздел)
  - 📝 Экзамены
  - 👥 Список комиссий
  - 🌳 Структура комиссий

**Верхнее меню (top):**
- ➕ Принять на работу
- 🏥 Выдать направление кандидату
- ⚙️ Администрирование

## Рекомендации

1. **Используйте пустое значение по умолчанию** — Если не выбраны пункты меню, пользователь видит все. Это упрощает начальную настройку.

2. **Разделители помогают структурировать меню** — Используйте `is_separator=True` для визуального разделения групп пунктов.

3. **Иерархия для дропдаунов** — Используйте `parent` для создания выпадающих подменю (будущая функциональность).

4. **Суперпользователи всегда видят все** — Это защищает от случайной блокировки административного доступа.

5. **Тестируйте с обычным пользователем** — После настройки прав войдите под тестовым пользователем для проверки.

## Troubleshooting

**Проблема:** Пользователь не видит пункты меню после настройки.

**Решение:**
1. Проверить, что пункты меню активны (`is_active=True`)
2. Проверить, что пункты выбраны в профиле пользователя
3. Проверить, что у пользователя есть профиль (создается автоматически через сигнал)

**Проблема:** Изменения в admin не отражаются в меню.

**Решение:**
1. Убедиться, что шаблон использует теги `{% load menu_tags %}`
2. Очистить кэш браузера (Ctrl+F5)
3. Перезапустить сервер разработки

**Проблема:** После удаления всех выбранных пунктов меню, пользователь ничего не видит.

**Решение:**
- Это ожидаемое поведение. Пустое значение = "видеть все", но если хотя бы один пункт выбран, применяются ограничения.
- Чтобы пользователь видел все, НЕ выбирайте пункты (оставьте список пустым).

## Дальнейшее развитие

Планируемые возможности:
- 🔄 Drag-and-drop сортировка пунктов меню в admin
- 📂 Вложенные выпадающие меню (dropdown)
- 🎨 Кастомные цвета и стили для пунктов
- 🔒 Интеграция с Django Permissions
- 📊 Статистика использования пунктов меню
