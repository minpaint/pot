# 📱 Руководство по мобильной адаптации OT_online

## Обзор

Система OT_online полностью адаптирована для мобильных устройств и планшетов (320px - 1024px). Все критические страницы поддерживают адаптивный дизайн с автоматическим переключением между табличным и карточным видом.

## Breakpoints

Проект использует 5 основных breakpoints:

| Breakpoint | Диапазон | Устройства | Описание |
|------------|----------|------------|----------|
| **xs** | 320px - 575px | Смартфоны портрет | Компактный карточный вид |
| **sm** | 576px - 767px | Смартфоны ландшафт | Оптимизированный вид |
| **md** | 768px - 991px | Планшеты портрет | Компактные таблицы |
| **lg** | 992px - 1199px | Планшеты ландшафт | Стандартный вид |
| **xl** | 1200px+ | Десктопы | Полный функционал |

## CSS файлы

### 1. `responsive.css` - Базовая инфраструктура

**Подключение:** Уже подключен в `base.html`

**Содержит:**
- CSS переменные для всех breakpoints
- Адаптивные размеры шрифтов через `clamp()`
- Touch-friendly константы
- iOS-специфичные фиксы
- Safe area insets для iPhone X+

**Пример использования:**
```css
/* Используйте CSS переменные в своих стилях */
.my-element {
    padding: var(--content-padding);
    font-size: var(--font-size-base);
    min-height: var(--touch-target-min);
}
```

### 2. `frontend_tree_view.css` - Древовидные таблицы

**Уже используется на:** Главная страница, списки сотрудников

**Особенности:**
- ✅ Автоматическое превращение в карточки на <768px
- ✅ Touch-friendly элементы (toggle 32px, checkbox 24px)
- ✅ Адаптивная actions bar
- ✅ Скрытие строк организаций на мобильных

**Не требует изменений в HTML** - работает автоматически!

### 3. `wizard_form.css` - Формы приема

**Используется на:** Формы приема на работу, wizard

**Особенности:**
- ✅ Вертикальный стек wizard steps на мобильных
- ✅ Все колонки на 100% ширину (<576px)
- ✅ Touch-friendly кнопки (min-height 48px)
- ✅ Select2 оптимизация (font-size 16px для iOS)

**Автоматическая работа** с формами `#hiring-form`.

### 4. `tables_responsive.css` ⭐ УНИВЕРСАЛЬНЫЙ ПАТТЕРН

**Как использовать:**

#### Шаг 1: Подключите CSS в вашем шаблоне
```html
{% load static %}
<link rel="stylesheet" href="{% static 'directory/css/tables_responsive.css' %}">
```

#### Шаг 2: Добавьте класс к таблице
```html
<table class="table responsive-table">
    <thead>
        <tr>
            <th>Имя</th>
            <th>Email</th>
            <th>Телефон</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td data-label="Имя">Иван Иванов</td>
            <td data-label="Email">ivan@example.com</td>
            <td data-label="Телефон">+7 999 123-45-67</td>
            <td data-label="Действия">
                <div class="btn-group">
                    <a href="#" class="btn btn-sm btn-primary">Редактировать</a>
                    <a href="#" class="btn btn-sm btn-danger">Удалить</a>
                </div>
            </td>
        </tr>
    </tbody>
</table>
```

**Важно:** Добавляйте атрибут `data-label` к каждой `<td>` - он станет заголовком поля в карточном виде!

#### Опциональные модификаторы:

```html
<!-- Компактные карточки -->
<table class="table responsive-table table-cards-compact">

<!-- Без лейблов полей -->
<table class="table responsive-table table-cards-no-labels">

<!-- С чекбоксами в первой колонке -->
<table class="table responsive-table has-checkboxes">
```

### 5. `documents_responsive.css` - Интерфейс документов

**Используется на:** Генерация, выбор и просмотр документов

**Особенности:**
- ✅ Document grid → список на мобильных
- ✅ Touch-friendly чекбоксы (24x24px)
- ✅ Адаптивные формы протоколов
- ✅ Оптимизация для печати

**Автоматическая работа** с классами `.document-grid`, `.document-card`.

## Критические моменты iOS

### ⚠️ Предотвращение zoom

**Проблема:** iOS Safari автоматически увеличивает масштаб при фокусе на input с font-size < 16px.

**Решение:** Везде используется `font-size: 16px !important` для:
- `input[type="text"]`
- `input[type="email"]`
- `input[type="password"]`
- `select`
- `.select2-selection`

### ⚠️ Touch-friendly размеры

**Правило:** Все интерактивные элементы минимум **44x44px** (стандарт iOS/Android).

**Реализовано для:**
- Кнопки (`min-height: 48px`)
- Чекбоксы (24x24px)
- Toggle переключатели (32x32px)
- Select2 dropdown (48px)

### ⚠️ Safe Area Insets

**Для iPhone X, 11, 12, 13 и новее:**

```css
@supports (padding: max(0px)) {
    .content-area {
        padding-left: max(1rem, env(safe-area-inset-left));
        padding-right: max(1rem, env(safe-area-inset-right));
        padding-bottom: max(1rem, env(safe-area-inset-bottom));
    }
}
```

Автоматически добавляет отступы для "вырезов" iPhone.

## Как адаптировать новую страницу

### 1. Используйте Bootstrap Grid

```html
<div class="container">
    <div class="row">
        <!-- На мобильных - 100%, на десктопе - 50% -->
        <div class="col-12 col-md-6">
            <h2>Заголовок</h2>
            <p>Контент</p>
        </div>

        <!-- На мобильных - 100%, на десктопе - 50% -->
        <div class="col-12 col-md-6">
            <h2>Заголовок</h2>
            <p>Контент</p>
        </div>
    </div>
</div>
```

### 2. Для таблиц - добавьте класс `responsive-table`

```html
<table class="table responsive-table">
    <!-- Не забудьте data-label для каждой td! -->
</table>
```

### 3. Для форм - используйте существующие стили

```html
<form id="my-form">
    <div class="row">
        <!-- Все колонки на 100% на мобильных автоматически -->
        <div class="col-md-6">
            <label>Поле 1</label>
            <input type="text" class="form-control">
        </div>
        <div class="col-md-6">
            <label>Поле 2</label>
            <select class="form-control">...</select>
        </div>
    </div>

    <!-- Кнопки -->
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Сохранить</button>
        <a href="#" class="btn btn-secondary">Отмена</a>
    </div>
</form>
```

### 4. Для кастомных компонентов - используйте media queries

```css
/* Desktop стили */
.my-component {
    display: flex;
    gap: 1rem;
}

/* Мобильные */
@media (max-width: 767px) {
    .my-component {
        flex-direction: column;
        gap: 0.5rem;
    }
}
```

## Тестирование

### Чек-лист для тестирования адаптивности:

#### Смартфоны (< 576px)
- [ ] Sidebar скрывается, открывается по кнопке
- [ ] Overlay закрывает sidebar при клике
- [ ] Таблицы превращаются в карточки
- [ ] Все кнопки минимум 44px высотой
- [ ] Формы на всю ширину
- [ ] Нет горизонтального скролла

#### Планшеты (768px - 991px)
- [ ] Sidebar 180px ширина
- [ ] Таблицы компактные, но не карточки
- [ ] Формы используют сетку

#### Десктопы (992px+)
- [ ] Все элементы в полном размере
- [ ] Sidebar 200px
- [ ] Стандартные таблицы

### Устройства для тестирования:

1. **iPhone SE (320px)** - минимальный размер
2. **iPhone 12/13 (390px)** - популярный размер
3. **iPad Mini (768px)** - планшет портрет
4. **iPad Pro (1024px)** - планшет ландшафт

### Chrome DevTools

Открыть DevTools → Toggle Device Toolbar (Ctrl+Shift+M) → Выбрать устройство

**Полезные пресеты:**
- iPhone SE
- iPhone 12 Pro
- iPad Air
- iPad Pro

## Часто задаваемые вопросы

### Q: Почему на iOS zoom при фокусе на input?
**A:** Проверьте, что `font-size` input равен минимум `16px`.

### Q: Таблица не превращается в карточки на мобильных
**A:** Убедитесь, что:
1. Подключен `tables_responsive.css`
2. У таблицы есть класс `responsive-table`
3. У всех `<td>` есть атрибут `data-label`

### Q: Sidebar не скрывается на планшете
**A:** Sidebar скрывается только на <768px. На планшетах 768-991px он компактный (180px).

### Q: Кнопка слишком маленькая для пальца
**A:** Добавьте `min-height: 48px` или используйте класс `btn` из Bootstrap.

### Q: Wizard steps не вертикальные на мобильных
**A:** Убедитесь, что подключен `wizard_form.css` и у шагов есть атрибут `data-step`.

## Производительность

### Оптимизация анимаций

Для пользователей с `prefers-reduced-motion`:
```css
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
    }
}
```

Автоматически отключает анимации для людей с вестибулярными нарушениями.

### Will-change оптимизация

Критические анимированные элементы используют `will-change`:
```css
.sidebar,
.sidebar-overlay,
.modal {
    will-change: transform;
}
```

## Поддержка браузеров

| Браузер | Минимальная версия | Статус |
|---------|-------------------|--------|
| Chrome Mobile | 90+ | ✅ Полная поддержка |
| Safari iOS | 14+ | ✅ Полная поддержка |
| Samsung Internet | 15+ | ✅ Полная поддержка |
| Firefox Mobile | 90+ | ✅ Полная поддержка |
| Opera Mobile | 60+ | ✅ Полная поддержка |

**Не поддерживаются:**
- Internet Explorer (все версии)
- Устройства с шириной экрана < 320px

## Дополнительные ресурсы

- [Bootstrap 5.3 Grid Documentation](https://getbootstrap.com/docs/5.3/layout/grid/)
- [CSS clamp() на MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/clamp)
- [iOS Safari Web Content Guide](https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariWebContent/Introduction/Introduction.html)

---

**Дата создания:** 2025-12-28
**Версия:** 1.0
**Автор:** Claude Code (Anthropic)
