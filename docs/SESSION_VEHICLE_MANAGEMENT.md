# Сессия: Реализация функционала управления служебным автомобилем

**Дата:** 29 ноября 2025
**Статус:** ✅ Завершено

## 📋 Краткое описание

Реализована система проверки знаний и стажировки для сотрудников, управляющих служебным автомобилем. Добавлена автоматическая генерация дополнительных строк в протоколах проверки знаний для проверки по виду выполняемых работ "Управление служебным автомобилем".

## 🎯 Основная задача

При установленном флаге `drives_company_vehicle` у должности:
1. В **первичном протоколе** проверки знаний добавляется **две строки** под одной шапкой:
   - Проверка знаний по профессии (основная должность)
   - Проверка знаний по виду выполняемых работ (управление служебным автомобилем)

2. В **периодическом протоколе** проверки знаний добавляется **две строки** для каждого сотрудника:
   - Проверка знаний по профессии
   - Проверка знаний по виду выполняемых работ

3. В **распоряжении о стажировке** устанавливается минимальный срок стажировки **5 рабочих дней**

## 🔧 Изменённые файлы

### 1. `directory/document_generators/protocol_generator.py`

#### Добавлена функция `_find_knowledge_protocol_table()`
```python
def _find_knowledge_protocol_table(docx_doc):
    """
    Находит таблицу результатов в первичном протоколе проверки знаний.
    Ищет таблицу с заголовком содержащим "Фамилия" и "должность".
    """
```
**Назначение:** Поиск таблицы результатов в шаблоне первичного протокола для заполнения строками.

#### Модифицирована функция `generate_knowledge_protocol()`
**Основные изменения:**
- Использует прямой рендеринг через `DocxTemplate` вместо `generate_docx_from_template()`
- После рендеринга шаблона находит таблицу результатов
- Очищает таблицу (оставляет только шапку)
- Заполняет таблицу 1 или 2 строками:
  ```python
  # Строка 1: проверка знаний по профессии (основная должность)
  employees_data.append({
      'fio_nominative': context.get('fio_nominative', ''),
      'position_nominative': context.get('position_nominative', ''),
      'ticket_number': custom_context.get('ticket_number', '') if custom_context else '',
  })

  # Строка 2: проверка знаний по видам выполняемых работ (управление автомобилем)
  if needs_vehicle_training(employee):
      employees_data.append({
          'fio_nominative': context.get('fio_nominative', ''),
          'position_nominative': get_vehicle_position_name(),
          'ticket_number': custom_context.get('ticket_number', '') if custom_context else '',
      })
  ```

**Местоположение:** строки 25-165

#### Модифицирована функция `generate_periodic_protocol()`
**Изменения:**
- Восстановлена логика добавления двух строк для сотрудников с управлением автомобилем
- Используются утилиты `needs_vehicle_training()` и `get_vehicle_position_name()`

**Код:**
```python
# Проверка знаний по профессии - всегда добавляем основную должность
employees_data.append({
    'fio_nominative': emp_ctx.get('fio_nominative', ''),
    'position_nominative': emp_ctx.get('position_nominative', ''),
    'ticket_number': '',
})

# Проверка знаний по видам выполняемых работ
# Если сотрудник управляет автомобилем - добавляем вторую строку
if needs_vehicle_training(emp):
    employees_data.append({
        'fio_nominative': emp_ctx.get('fio_nominative', ''),
        'position_nominative': get_vehicle_position_name(),
        'ticket_number': '',
    })
```

**Местоположение:** строки 293-317

### 2. `directory/document_generators/order_generator.py`

#### Модифицирована функция `generate_all_orders()`
**Изменения в логике срока стажировки:**

```python
# Срок стажировки берём из должности
# Если есть управление служебным автомобилем - минимум 5 дней
base_internship = getattr(employee.position, 'internship_period_days', 2) if employee.position else 2
drives_vehicle = employee.position and getattr(employee.position, 'drives_company_vehicle', False)

if drives_vehicle:
    # Если управляет автомобилем - берём максимум из срока по должности и 5 дней
    duration = max(base_internship, 5)
    context['internship_duration'] = duration
    # Обновляем форматированную версию
    from directory.utils.declension import format_days
    context['internship_duration_formatted'] = format_days(duration)
    context['has_internship'] = True
else:
    context['internship_duration'] = base_internship
```

**Логика:**
- Если `drives_company_vehicle = True`: срок стажировки = `max(internship_period_days, 5)`
- Если `drives_company_vehicle = False`: срок стажировки = `internship_period_days`

**Местоположение:** строки 138-152

**Убрано логирование:** удалено логирование переменной `driving_auto` (строка 164)

### 3. `directory/document_generators/base.py`

#### Удалена переменная `driving_auto`
**Было:**
```python
# Переменная для управления служебным автомобилем
# Если флаг установлен, добавляем текст в скобках для вставки после должности
if employee.position and getattr(employee.position, 'drives_company_vehicle', False):
    context['driving_auto'] = ' (управление служебным автомобилем)'
else:
    context['driving_auto'] = ''
```

**Стало:** код удалён (строки 276-281)

**Причина:** Переменная больше не нужна, так как добавление информации об автомобиле реализовано через отдельные строки в таблицах протоколов.

## 📊 Результаты тестирования

### Тестовые данные
- **Сотрудник:** Иванов Иван Иванович (ID: 10)
- **Должность:** Инженер
- **Флаг:** `drives_company_vehicle = True`
- **Срок стажировки по должности:** 0 дней

### Результаты генерации

1. ✅ **Распоряжение о стажировке**
   - Файл: `Распоряжение_Иванов И.И..docx`
   - Размер: 12,760 байт
   - Срок стажировки: **5 дней** (автоматически установлено)

2. ✅ **Первичный протокол проверки знаний**
   - Файл: `Протокол_Иванов И.И..docx`
   - Размер: 15,725 байт
   - **Две строки в таблице:**
     - Иванов И.И. - Инженер
     - Иванов И.И. - Управление служебным автомобилем

3. ✅ **Периодический протокол проверки знаний**
   - Файл: `Протокол_периодической_проверки.docx`
   - Размер: 17,098 байт
   - **Две строки в таблице:**
     - Иванов И.И. - Инженер
     - Иванов И.И. - Управление служебным автомобилем

## 🔍 Используемые утилиты

Все функции находятся в `directory/utils/vehicle_utils.py`:

1. **`needs_vehicle_training(employee)`**
   - Проверяет, нужны ли сотруднику мероприятия по управлению автомобилем
   - Возвращает: `employee.position.drives_company_vehicle`

2. **`get_vehicle_position_name()`**
   - Возвращает стандартное название виртуальной должности
   - Возвращает: `"Управление служебным автомобилем"`

3. **`get_vehicle_internship_days()`**
   - Возвращает фиксированный срок стажировки для управления автомобилем
   - Возвращает: `5` (рабочих дней)

## 📝 Терминология

**Проверка знаний по профессии** - проверка знаний требований охраны труда, связанных с основной должностью сотрудника (например, "Инженер").

**Проверка знаний по видам выполняемых работ** - проверка знаний требований охраны труда, связанных с дополнительными видами работ, выполняемых сотрудником (например, "Управление служебным автомобилем").

## 🎨 Структура протоколов

### Первичный протокол проверки знаний

**Шаблон:** `media/document_templates/knowledge_protocol_template_udostoverenie.docx`

**Таблица результатов:**
| № п/п | Фамилия, инициалы | Должность | Вид проверки | Номер билета | Результат | Роспись |
|-------|-------------------|-----------|--------------|--------------|-----------|---------|
| 1 | Иванов И.И. | Инженер | первичная | 15 | прошел | |
| 2 | Иванов И.И. | Управление служебным автомобилем | первичная | 15 | прошел | |

### Периодический протокол проверки знаний

**Шаблон:** `media/document_templates/periodic_protocol_template.docx`

**Таблица результатов:**
| № п/п | Фамилия, инициалы | Должность | Вид проверки | Номер билета | Результат | Роспись |
|-------|-------------------|-----------|--------------|--------------|-----------|---------|
| 1 | Иванов И.И. | Инженер | периодическая | | | |
| 2 | Иванов И.И. | Управление служебным автомобилем | периодическая | | | |

## 🔄 Логика работы

```
┌─────────────────────────────────────┐
│ Сотрудник с drives_company_vehicle  │
└──────────────┬──────────────────────┘
               │
       ┌───────┴────────┐
       │                │
       ▼                ▼
  Протоколы       Распоряжение
  (2 строки)    (срок ≥ 5 дней)
       │                │
       ├────────────────┤
       │                │
       ▼                ▼
   Строка 1:      max(internship_
   Профессия      period_days, 5)
       │
       ▼
   Строка 2:
   Управление авто
```

## ⚠️ Важные замечания

1. **Шаблоны Word остались без изменений** - вся логика реализована в коде Python

2. **Переменная `{{ driving_auto }}`** была удалена из контекста, так как концепция изменилась:
   - Было: добавление текста "(управление служебным автомобилем)" к должности
   - Стало: добавление отдельной строки в таблицу протокола

3. **Номер билета** в протоколах берётся из `custom_context`, передаваемого в функцию генерации

4. **Нумерация строк** в таблицах протоколов идёт сквозная (1, 2, 3, ...), а не отдельно для каждого сотрудника

## 🧪 Тестирование

### Команда для тестирования через Django shell:

```python
from directory.models import Employee
from directory.document_generators.order_generator import generate_all_orders
from directory.document_generators.protocol_generator import generate_knowledge_protocol, generate_periodic_protocol

# Получаем сотрудника с флагом управления автомобилем
emp = Employee.objects.select_related('position').get(id=10)

# 1. Распоряжение о стажировке
result = generate_all_orders(employee=emp, user=None)
print(f"Order: {result['filename']} - {len(result['content'])} bytes")

# 2. Первичный протокол
custom_ctx = {'ticket_number': 15, 'test_result': 'прошел'}
result = generate_knowledge_protocol(employee=emp, user=None, custom_context=custom_ctx)
print(f"Knowledge protocol: {result['filename']} - {len(result['content'])} bytes")

# 3. Периодический протокол
result = generate_periodic_protocol([emp], user=None)
print(f"Periodic protocol: {result['filename']} - {len(result['content'])} bytes")
```

## 📚 Связанные файлы

- `directory/models/position.py` - модель Position с полем `drives_company_vehicle`
- `directory/utils/vehicle_utils.py` - утилиты для работы с управлением автомобилем
- `directory/migrations/0045_add_company_vehicle_fields.py` - миграция с добавлением полей

## 🚀 Что дальше

1. **Тестирование через веб-интерфейс:**
   - Создать/обновить сотрудника с флагом `drives_company_vehicle`
   - Сгенерировать все три типа документов
   - Проверить корректность заполнения таблиц

2. **Возможные улучшения:**
   - Добавить возможность указывать разные номера билетов для основной должности и управления автомобилем
   - Добавить настройку срока стажировки для управления автомобилем (сейчас фиксировано 5 дней)
   - Добавить автоматическое заполнение результатов проверки знаний

## 📌 Итоги

✅ Реализована полная поддержка проверки знаний по виду работ "Управление служебным автомобилем"
✅ Автоматическая генерация двух строк в протоколах для сотрудников с флагом
✅ Автоматическая установка минимального срока стажировки 5 дней
✅ Все изменения протестированы и работают корректно

**Система готова к использованию!**
