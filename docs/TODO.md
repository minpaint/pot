# Статус и TODO по ключевым срокам (для продолжения сессии)

## ✅ Что уже сделано и проверено (13.12.2024)

### Миграции и структура данных
- ✅ Категории переведены в справочник без привязки к организациям; дубликаты объединяются миграцией `0028_restructure_to_reference_categories`.
- ✅ Мероприятия (`KeyDeadlineItem`) теперь привязаны к организации, могут переопределять `periodicity_months`; расчёт дат берёт значение из мероприятия или из категории.
- ✅ Все 28 миграций успешно применены на домашней машине
- ✅ `py manage.py check` проходит без ошибок

### Админка
- ✅ В админке организации добавлен инлайн "Ключевые сроки" с датами, статусом и переопределяемой периодичностью
- ✅ Существующий раздел KeyDeadlineItem показывает организацию и фильтры по ней
- ✅ KeyDeadlineCategoryAdmin работает корректно для управления справочником категорий
- ✅ OrganizationKeyDeadlinesAdmin предоставляет удобный интерфейс для работы с мероприятиями через организацию

### Команды и рассылки
- ✅ Команда `send_key_deadline_notifications` обновлена: выборка по `organization`, периодичность с учётом переопределений
- ✅ Команда работает корректно, поддерживает параметры: `--emails`, `--organization`, `--warning-days`
- ✅ Убраны проблемные emoji в консольном выводе
- ✅ Контекст-процессор обновлён для работы с новой структурой

### Зависимости
- ✅ Установлены все зависимости из requirements.txt включая pymorphy3-2.0.4 и pymorphy3-dicts-ru

## 📋 Что осталось сделать

### 1. Проверка данных в админке (на продакшене)
- 🔲 В админке категорий (`/admin/deadline_control/keydeadlinecategory/`) проверить, что остались уникальные категории-справочник, без привязки к организациям
- 🔲 В админке организаций открыть пару карточек и убедиться, что инлайн "Ключевые сроки" показывает мероприятия с нужной организацией и датами
- 🔲 Проверить корректность расчёта дат с учётом переопределённой периодичности

### 2. Наполнение справочника (при необходимости)
- 🔲 Скорректировать список категорий (иконки, названия, эталонная периодичность)
- 🔲 В карточках организаций через инлайн добавить/скорректировать мероприятия
- 🔲 При необходимости задать индивидуальную `periodicity_months` (если пусто — используется эталон категории)

### 3. Тестирование рассылок (на продакшене)
- 🔲 Протестировать команду с реальными данными:
  ```bash
  py manage.py send_key_deadline_notifications --emails test@example.com --organization <ID>
  ```
- 🔲 Убедиться, что письма формируются с учётом переопределённой периодичности
- 🔲 Проверить, что логи (`KeyDeadlineSendLog` в админке) фиксируют отправителя, получателей, даты и статус
- 🔲 Проверить HTML-шаблоны писем (группировка по категориям, корректное отображение)

### 4. Навигация в админке (опционально)
- 🔲 Решить, нужен ли отдельный раздел `Ключевые сроки` (KeyDeadlineItem) в меню
- 🔲 Если оставить только работу через карточку организации — можно скрыть из меню или оставить для просмотра всех мероприятий

### 5. Финальная проверка
- 🔲 Smoke-тест: создание/редактирование мероприятий через инлайн в организации
- 🔲 Проверить контекст-процессоры и уведомления в интерфейсе
- 🔲 Убедиться, что нет сломанных ссылок или старых зависимостей
